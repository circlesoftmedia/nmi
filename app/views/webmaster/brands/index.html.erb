<div id="page-wrapper">
  <div id="page-inner">
    <div class="row">
      <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="panel panel-default">
          <div class="panel-heading">
             Brand Lists
          </div>
          <div class="panel-body">
            <div class="table-responsive">

              <table class="table table-striped table-bordered table-hover">
                <tr>
                  <th>Name</th>
                  <th></th>
                  <th></th>
                  <th></th>
                </tr>

              <% @brands.each do |b| %>
                <tr>
                  <td><%= b.name %></td>
                  <td><%= link_to 'Show', webmaster_brand_path(b) %></td>
                  <td><%= link_to 'Edit', edit_webmaster_brand_path(b) %></td>
                  <td><%= link_to 'Destroy', webmaster_brand_path(b), confirm: 'Are you sure?', method: :delete %></td>
                </tr>
              <% end %>
              </table>
            </div>
            <%= link_to 'New', new_webmaster_brand_path %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>